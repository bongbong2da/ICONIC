{
  "engines": {
    "node": "15.12.0"
  },
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm install && npm run build && npx serve -s build"
  }
}

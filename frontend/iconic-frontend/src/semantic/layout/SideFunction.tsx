import React from 'react';
import {Button, Container} from "semantic-ui-react";

const SideFunction = () => {
    return (
        <Container>
            <Button>
                채널 만들기
            </Button>
        </Container>
    )
}